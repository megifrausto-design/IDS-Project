IDS-Project/
├── README.md          # project overview, goals checklist, tech stack table, status
├── .gitignore          # covers Python/Java build artifacts + keeps raw pcap/csv data out of git
├── docs/
│   └── architecture.md # open questions to resolve once approach is picked
├── src/                # empty for now, ready for code
├── data/                # empty, for sample captures/datasets (gitignored)
└── tests/               # empty, for test cases
